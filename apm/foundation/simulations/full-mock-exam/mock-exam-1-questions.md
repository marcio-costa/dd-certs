# Full Mock Exam #1 — 75 Questions / 2 Hours

> Time yourself strictly. Single-answer multiple choice only. Answer key is in `mock-exam-1-answers.md` — don't peek.

---

## Section A — Domain 1: APM Fundamentals (Q1–Q19)

**Q1.** Which of the following best describes a **trace** in Datadog APM?

A) A single function call captured in code.
B) A directed graph of spans representing one end-to-end request, possibly across many services.
C) An aggregated metric of request count by service.
D) A log line correlated to a service.

---

**Q2.** What is the default port the trace-agent listens on for incoming spans?

A) 8125 / UDP
B) 8126 / TCP
C) 4317 / gRPC
D) 4318 / HTTP

---

**Q3.** Unified Service Tagging is composed of these three tags:

A) `app`, `tier`, `region`
B) `env`, `service`, `version`
C) `team`, `cluster`, `pod`
D) `host`, `container`, `namespace`

---

**Q4.** Ingested spans (the Live Search window) live for:

A) 5 minutes
B) 15 minutes
C) 1 hour
D) 24 hours

---

**Q5.** Custom retention filters keep matching indexed spans for:

A) 1 hour
B) 24 hours
C) 15 days
D) 90 days

---

**Q6.** Datadog's intelligent retention filter keeps indexed spans for:

A) 7 days
B) 15 days
C) 30 days
D) 90 days

---

**Q7.** Trace metrics are retained for:

A) 15 minutes
B) 15 days
C) 6 months
D) 15 months

---

**Q8.** A team uses head-based sampling. Where is the keep/drop decision made?

A) At every service in the trace, independently.
B) At the root span, then propagated downstream via context.
C) At the Datadog backend after ingest.
D) After all spans complete.

---

**Q9.** Which language **does not** rely on bytecode rewriting for auto-instrumentation?

A) Java
B) Python
C) Go
D) .NET

---

**Q10.** A "community tracer" most commonly refers to:

A) A Datadog `dd-trace-*` library.
B) An OpenTelemetry SDK with the Datadog exporter.
C) DogStatsD.
D) The Datadog Cluster Agent.

---

**Q11.** Auto-version with Agent 7.52+ tries which value first?

A) Container image tag.
B) Explicit `DD_VERSION`.
C) Git commit SHA.
D) Hostname.

---

**Q12.** Which architecture is recommended for AWS Lambda APM tracing?

A) Install the Datadog Agent in the Lambda execution environment.
B) Use the Datadog Lambda Extension layer.
C) Send traces directly from the function to the Datadog backend.
D) Deploy a Cluster Agent in the Lambda VPC.

---

**Q13.** Which is a core advantage of an OOTB Datadog tracer over a community OTel SDK?

A) It works without the Agent.
B) Broader auto-instrumentation coverage and immediate access to new Datadog features.
C) It supports more languages than OTel.
D) It bypasses sampling entirely.

---

**Q14.** A **resource** in Datadog APM terminology is best described as:

A) The Datadog Agent process.
B) A specific action within a service (e.g., `GET /checkout`).
C) A custom metric.
D) A Kubernetes pod.

---

**Q15.** Which of these statements about USM (Unified Service Tagging) is correct?

A) USM unifies APM, logs, processes, and infrastructure metrics through `env`/`service`/`version`.
B) USM only applies to APM data.
C) USM tags are auto-generated by Datadog and cannot be customized.
D) USM tags must be set in `datadog.yaml`, not on the application.

---

**Q16.** Which is **true** about trace context propagation?

A) Each service generates a fresh trace ID per request.
B) Trace IDs are passed between services (typically via HTTP headers like `traceparent`) so spans can be stitched together.
C) Datadog correlates spans by host name only.
D) Trace context is only required for synchronous calls.

---

**Q17.** Which DogStatsD port is the default?

A) 8125 / UDP
B) 8126 / TCP
C) 4317 / gRPC
D) 9092 / TCP

---

**Q18.** A team uses Agent 7.20 in Docker. By default, will the trace-agent accept traces from app containers?

A) No — `DD_APM_NON_LOCAL_TRAFFIC` defaults to `false`.
B) Yes — `DD_APM_NON_LOCAL_TRAFFIC` defaults to `true` since Agent 7.18.
C) Only if a custom port is set.
D) Only with the OTel Collector path.

---

**Q19.** Which is **true** about trace metrics?

A) They are sampled at 10%.
B) They are computed from 100% of ingested spans, regardless of sampling/retention.
C) They are only available for error spans.
D) They are deleted when retention filters change.

---

## Section B — Domain 2: Application Instrumentation (Q20–Q41)

**Q20.** Which env var enables the Continuous Profiler in most languages?

A) `DD_PROFILE_ON`
B) `DD_PROFILING_ENABLED`
C) `DD_TRACE_PROFILE`
D) `DD_PROFILER`

---

**Q21.** The modern, JSON-based env var to define per-service sampling rules is:

A) `DD_TRACE_SAMPLE_RATE`
B) `DD_SAMPLER_RULES`
C) `DD_TRACE_SAMPLING_RULES`
D) `DD_APM_SAMPLE_PCT`

---

**Q22.** Where does **SQL obfuscation** for APM spans run?

A) In the application driver.
B) In the trace-agent.
C) In the Datadog backend.
D) In the database itself.

---

**Q23.** A team needs to drop traces matching `GET /healthz`. The right knob is:

A) `DD_APM_REPLACE_TAGS`
B) `DD_APM_IGNORE_RESOURCES='GET /healthz'`
C) `DD_TRACE_DROP_RESOURCE`
D) `DD_APM_NON_LOCAL_TRAFFIC=false`

---

**Q24.** Which env var instructs the trace-agent to accept connections from non-localhost addresses?

A) `DD_APM_REMOTE_ENABLED`
B) `DD_APM_NON_LOCAL_TRAFFIC`
C) `DD_APM_REMOTE_ACCEPT`
D) `DD_BIND_ALL`

---

**Q25.** Which knob is the canonical way to **scrub a sensitive value** from arbitrary span tags?

A) `DD_APM_REPLACE_TAGS`
B) `DD_TAG_OBFUSCATE`
C) `DD_APM_SECURE_MODE`
D) `DD_OBFUSCATE_TAGS`

---

**Q26.** A Java application enables auto-instrumentation. Which JVM flag is required?

A) `-Ddd.trace.enabled=true`
B) `-javaagent:dd-java-agent.jar`
C) `-Xtrace=datadog`
D) `-Ddd.classloader=true`

---

**Q27.** The default `DD_APM_TARGET_TPS` value is:

A) 1
B) 10
C) 100
D) 1000

---

**Q28.** Which feature lets you add new spans, log lines, or metrics to a running production app **without redeploying**?

A) Single-Step Instrumentation
B) Manual Instrumentation
C) Dynamic Instrumentation
D) DogStatsD events

---

**Q29.** Which is recommended for a Kubernetes pod to point at the local node's Agent?

A) Hardcode `127.0.0.1:8126`.
B) Set `DD_AGENT_HOST=status.hostIP` via downward API, or use a UDS hostPath.
C) Set `DD_AGENT_HOST=0.0.0.0`.
D) Use the Datadog API key directly from the app.

---

**Q30.** Which is the canonical **UDS path** the Agent uses for APM in Kubernetes?

A) `/var/run/datadog/dsd.socket`
B) `/var/run/datadog/apm.socket`
C) `/var/run/agent/trace.sock`
D) `/var/lib/datadog/apm.sock`

---

**Q31.** A team uses Single-Step Instrumentation in Kubernetes. Which Datadog component injects the tracer at pod creation?

A) The trace-agent.
B) The Cluster Agent + Admission Controller.
C) DogStatsD.
D) The OpenTelemetry Collector.

---

**Q32.** Runtime metrics (JVM, Python, Node) are emitted from the application:

A) Over 8126/TCP with traces.
B) Over DogStatsD UDP/8125 when enabled.
C) Directly to the Datadog backend over HTTPS.
D) They are not collected by Datadog.

---

**Q33.** A retention filter is intended to:

A) Drop traces with PII at ingest time.
B) Decide which sampled spans are kept long-term in indexed storage.
C) Reduce CPU on the trace-agent.
D) Disable sampling for a specific service.

---

**Q34.** Which env var is the simplest way to enable trace-log correlation?

A) `DD_TRACE_LOG_INJECT`
B) `DD_LOGS_INJECTION`
C) `DD_LOG_CORRELATE`
D) `DD_TRACE_CORRELATE_LOGS`

---

**Q35.** A team wants to keep all error traces for `payment-api` searchable for 15 days. The minimum required configuration is:

A) Set `DD_TRACE_SAMPLE_RATE=1.0` only.
B) Add a custom retention filter matching `service:payment-api status:error`.
C) Disable the intelligent retention filter.
D) Increase `DD_APM_TARGET_TPS` to 1000.

---

**Q36.** Which is **true** about the Continuous Profiler?

A) It runs only during development.
B) It samples in production at a target overhead of about 1% CPU.
C) It must be installed as a separate binary.
D) It replaces APM tracing.

---

**Q37.** A team needs to drop traces missing the `env:prod` tag. The correct knob is:

A) `DD_APM_FILTER_TAGS_REQUIRE='env:prod'`
B) `DD_APM_REJECT_TAGS='env:prod'`
C) `DD_TAG_REQUIRE_ENV=prod`
D) `DD_APM_DROP_NON_PROD=true`

---

**Q38.** Sampling and retention in Datadog APM are:

A) The same thing.
B) Independent — sampling is an ingest-side decision; retention is a long-term-storage decision.
C) Both performed in the application.
D) Both performed in the Datadog backend exclusively.

---

**Q39.** Which is a benefit of **Single-Step Instrumentation** vs manual instrumentation?

A) Better custom span granularity.
B) No code changes required to instrument supported applications.
C) It works without the Datadog Agent.
D) It allows OTel exporters in any language.

---

**Q40.** A Python team installs `ddtrace` and runs `ddtrace-run python app.py`. This is an example of:

A) Manual instrumentation.
B) Automatic instrumentation (library-level).
C) Single-Step Instrumentation.
D) Dynamic Instrumentation.

---

**Q41.** Which env var disables the trace-agent entirely?

A) `DD_APM_ENABLED=false`
B) `DD_TRACE_OFF=1`
C) `DD_APM_DISABLE=1`
D) `DD_APM_RECEIVER_PORT=0`

---

## Section C — Domain 3: Insight Discovery (Q42–Q63)

**Q42.** A service appears in the Software Catalog when:

A) Only when an `entity.datadog.yaml` is committed to a connected repo.
B) When APM traces with a recognized `service` tag arrive in Datadog (and/or YAML/OTel attributes).
C) Only when an admin manually adds it via API.
D) Only after PagerDuty integration is set up.

---

**Q43.** The Software Catalog map's two main layouts are:

A) Tree and Hierarchy
B) Cluster and Flow
C) Heatmap and Sankey
D) Service and Resource

---

**Q44.** The custom span tag query syntax in the Trace Explorer uses which prefix?

A) `#`   B) `$`   C) `@`   D) `&`

---

**Q45.** Which Trace Explorer query finds errors in `cart` for version `2025-10-01`?

A) `cart status:error version:2025-10-01`
B) `service:cart status:error version:2025-10-01`
C) `service:cart @status:err @version:2025-10-01`
D) `cart-2025-10-01-errors`

---

**Q46.** Live Search vs Indexed Search — which statement is **false**?

A) Live Search covers the last 15 minutes.
B) Live Search can query any tag, even non-indexed ones.
C) Indexed Search can query spans up to 15 days back if retained.
D) Indexed Search bypasses retention filters.

---

**Q47.** The default visualization in the Continuous Profiler is the:

A) Heat map
B) Pie chart
C) Flame graph
D) Sankey

---

**Q48.** Which feature compares CPU profiles from two different deployment versions side-by-side?

A) Live Tail
B) Profile Comparison view
C) Watchdog
D) Service Map

---

**Q49.** Auto-version (Agent 7.52+) priority order is:

A) Image tag > `DD_VERSION` > Git SHA
B) `DD_VERSION` > image tag > Git SHA
C) Git SHA > `DD_VERSION` > image tag
D) Hostname > `DD_VERSION` > image tag

---

**Q50.** Where do you compare metrics between two deployments side-by-side?

A) Trace Explorer
B) Service Page → Deployments tab
C) Software Catalog → Cost
D) Notebooks only

---

**Q51.** Error Tracking groups errors into **issues** by computing a fingerprint based on:

A) `error.type` + `error.message` + stack frames.
B) IP and timestamp.
C) `service` and `env` tags only.
D) HTTP status code only.

---

**Q52.** Override Error Tracking grouping with which span tag?

A) `error.group`
B) `error.fingerprint`
C) `error.cluster_id`
D) `error.bucket`

---

**Q53.** Resolved Error Tracking issues that recur after a deploy are:

A) Suppressed.
B) Auto-reopened.
C) Logged separately as new issues.
D) Sent only to Watchdog.

---

**Q54.** The Span Summary table shows for each span:

A) Only average duration.
B) Avg occurrences/trace, % traces with the span, avg duration, avg % execution time.
C) Stack traces of every instance.
D) Per-user counts.

---

**Q55.** Which metric naming convention applies to OOTB trace metrics?

A) `apm.<service>.<metric>`
B) `trace.<operation>.<metric>` (e.g. `trace.http.request.duration`)
C) `dd.<env>.<service>.<metric>`
D) `service.<service>.<metric>`

---

**Q56.** A custom span-based metric is billed as:

A) Free, like trace metrics.
B) A custom metric (counted per unique tag combination).
C) An indexed span.
D) An additional log line.

---

**Q57.** Which OOTB Service Page chart receives Watchdog anomaly overlays?

A) CPU usage only.
B) Requests/sec, latency, and error rate.
C) Disk I/O.
D) None — Watchdog appears only in Stories.

---

**Q58.** A user opens Trace Explorer with a 7-day window. They are now in:

A) Live Search.
B) Indexed (Retained) Search; only spans kept by retention filters are visible.
C) Trace metrics view.
D) Logs Explorer.

---

**Q59.** Which is the canonical visualization of service-to-service dependencies?

A) Notebook
B) Flame graph
C) Service Map
D) Hostmap

---

**Q60.** A Service Definition v2.2 specifies:

A) Service name only.
B) Service name, team, tier, languages, contacts, links, integrations, and tags.
C) Just the source repository URL.
D) Only PagerDuty integration.

---

**Q61.** A team wants to keep all `POST /checkout` traces searchable for 15 days. They should:

A) Add a custom retention filter matching `service:checkout resource_name:"POST /checkout"`.
B) Set `DD_TRACE_SAMPLE_RATE=1`.
C) Disable the intelligent retention filter.
D) Use Live Search only.

---

**Q62.** Which Trace Explorer query finds spans where `@duration` is between 200 ms and 1 s?

A) `@duration:[200ms TO 1s]`
B) `@duration:>200ms AND @duration:<1s`
C) Both A and B work.
D) `duration:200..1000`

---

**Q63.** A service has `dd-service: shopping-cart` in its YAML but emits APM traces with `service:shoppingcart`. What happens?

A) Datadog auto-reconciles the names.
B) The Software Catalog creates two distinct entries that won't link.
C) Only the YAML version is used.
D) Only the APM version is used.

---

## Section D — Domain 4: Troubleshooting Application using APM (Q64–Q75)

**Q64.** During a live incident, the FIRST APM page to open is typically:

A) Watchdog Stories list.
B) Service Page for the affected service, scoped to the incident time window.
C) Notebooks editor.
D) Logs Explorer with no filter.

---

**Q65.** "Critical Path" in the trace waterfall view highlights:

A) The first error span.
B) The longest causal path from root to leaf.
C) The path with the most child spans.
D) The slowest database query.

---

**Q66.** Which monitor type is best for "fire when p95 > 800 ms"?

A) Threshold alert on `trace.<op>.duration` (p95).
B) Anomaly alert.
C) Forecast alert.
D) Outlier alert.

---

**Q67.** Watchdog automatically scans services for anomalies on:

A) Disk and memory only.
B) Error rate, latency, and hits (request rate).
C) Container restarts.
D) Network latency between hosts.

---

**Q68.** Watchdog suppresses anomaly detection for endpoints below approximately:

A) 0.05 req/s
B) 0.5 req/s
C) 5 req/s
D) 50 req/s

---

**Q69.** Which monitor type detects deviation from a learned seasonal pattern?

A) Threshold
B) Anomaly
C) Forecast
D) Outlier

---

**Q70.** Default automatic APM error-rate threshold is:

A) 1%
B) 5%
C) 10%
D) 25%

---

**Q71.** A team needs to silence a monitor during a planned migration. The right tool is:

A) Delete the monitor.
B) Schedule a downtime.
C) Decrease the threshold.
D) Mute the host.

---

**Q72.** The simplest way to enable trace-log correlation in supported tracers is:

A) `DD_TRACE_LOG_INJECT`
B) `DD_LOGS_INJECTION=true`
C) Add `dd.trace_id` manually to every log call.
D) Use a custom log pipeline.

---

**Q73.** The W3C standard trace context header is:

A) `x-datadog-trace-id`
B) `traceparent`
C) `x-correlation-id`
D) `b3`

---

**Q74.** Which monitor type queries individual indexed spans rather than aggregated trace metrics?

A) APM Metric Monitor.
B) APM Trace Analytics Monitor.
C) Composite Monitor.
D) Service-level Monitor.

---

**Q75.** During an incident at 4:00 AM you need to look at traces from 6 hours ago. Which Trace Explorer mode are you in?

A) Live Search.
B) Indexed (Retained) Search; the trace must have been retained.
C) Trace metrics view.
D) Notebooks.

---

# END OF MOCK EXAM

Stop the timer. Open `mock-exam-1-answers.md` to grade.
